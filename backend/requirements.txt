# Core Flask dependencies
Flask==2.3.3
Flask-CORS==4.0.0
Flask-SQLAlchemy==3.0.5

# Database drivers (PostgreSQL - choose one that works)
# Try these in order - comment out the ones that don't work
# psycopg2-binary==2.9.9
# psycopg2==2.9.9
# pg8000>=1.29.0  # Pure Python PostgreSQL driver (fallback)

# For development, we'll start with SQLite (no additional driver needed)
# Uncomment one of the above when ready for PostgreSQL

# HTTP requests and data processing
requests==2.31.0
pandas==2.0.3
pytz==2023.3

# Configuration and environment
python-dotenv==1.0.0

# Background tasks (optional - will be handled gracefully if not available)
# APScheduler==3.10.4

# Data visualization and analysis
plotly==5.15.0
numpy==1.24.3

# Machine learning (optional for advanced analytics)
# scikit-learn==1.3.0
